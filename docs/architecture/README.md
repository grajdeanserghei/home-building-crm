# Architecture

System architecture, diagrams, and technical decisions.

The solution is a .NET Aspire application:

- **AppHost** — orchestrates the distributed application.
- **ApiService** — backend API.
- **ServiceDefaults** — shared service configuration (telemetry, health checks, resilience).

Add Architecture Decision Records (ADRs) here as `NNNN-short-title.md`.
