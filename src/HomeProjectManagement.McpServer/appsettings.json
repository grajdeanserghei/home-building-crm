{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "//CloudflareAccess": "Validates the Cloudflare Access assertion at the origin. Supply via user-secrets / environment, never commit real values. Set Enabled=true once the container sits behind the Cloudflare Tunnel + Access (Managed OAuth handles the Google login and the edge allow-list).",
  "CloudflareAccess": {
    "Enabled": false,
    "TeamDomain": "",
    "Audience": "",
    "AllowedEmails": []
  }
}
